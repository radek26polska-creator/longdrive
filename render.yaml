services:
  # BACKEND API - serwer Node.js
  - type: web
    name: longdrive-backend
    runtime: node
    buildCommand: cd server && npm install
    startCommand: node server.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: longdrive-db
          property: connectionString
    healthCheckPath: /health

  # FRONTEND - aplikacja React SPA
  - type: web
    name: longdrive-frontend
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://longdrive-backend.onrender.com/api
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache, no-store, must-revalidate
      - path: /assets/*
        name: Cache-Control
        value: public, max-age=31536000, immutable

# BAZA DANYCH - PostgreSQL (darmowy plan)
databases:
  - name: longdrive-db
    plan: free
    databaseName: longdrive
    user: longdrive_user
